import styled from "styled-components";

const StyledSongsContainer = styled.section`
    .heading {
        font-size: 2rem;
        margin-bottom: .5em;
        font-weight: 400;
    }
`

export default StyledSongsContainer